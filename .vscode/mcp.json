{
  "servers": {
    "ansible-devtools-mcp": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "ansible-devtools-mcp", "serve", "--stdio"],
      "env": {
        "WORKSPACE_ROOT": "${workspaceFolder}"
      }
    }
  }
}
